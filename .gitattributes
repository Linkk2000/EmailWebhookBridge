# 仓库内统一以 LF 存储。检出时按平台惯例转换：Windows 得到 CRLF，类 Unix 得到 LF。
# 加这个文件是因为此前仓库存 LF、工作区却是 CRLF，导致 git status 里三十多个文件
# 长期显示 modified 但内容毫无变化，稍不注意 git add -A 就会把整个仓库的换行符翻掉。
* text=auto

# 这几类必须始终是 LF，CRLF 会导致类 Unix 环境下执行失败或解析异常
*.sh   text eol=lf
*.py   text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf

# 二进制，禁止任何换行符转换
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.svg   text
*.jar   binary
*.war   binary
*.zip   binary
*.gz    binary
*.woff  binary
*.woff2 binary
*.ttf   binary
*.eot   binary
